<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Browser</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #111;
        color: #fff;
        margin: 0;
        padding: 20px;
      }
      a {
        color: #0bf;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      h1 {
        margin-bottom: 10px;
      }
      .breadcrumb {
        margin-bottom: 20px;
      }
      .item {
        padding: 10px;
        margin: 5px 0;
        background: #222;
        border-radius: 5px;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .item:hover {
        background: #333;
      }
      .item .name {
        flex: 1;
      }
      .type {
        font-size: 12px;
        opacity: 0.6;
        margin-left: 10px;
      }
    </style>
  </head>
  <body>
    <h1>📁 File Browser</h1>

    <div class="breadcrumb">
      <a href="/">Home</a>
      <% if (path) {
        const parts = path.split('/').filter(Boolean);
        let partial = ''; %>
        <% parts.forEach((part, index) => {
          partial += '/' + part;
          const isLast = index === parts.length - 1; %>
          ›
          <% if (isLast) { %>
            <span><%= part %></span>
          <% } else { %>
            <a href="/files?path=<%= encodeURIComponent(partial) %>"><%= part %></a>
          <% } %>
        <% }); %>
      <% } %>
    </div>

    <% if (entries.length === 0) { %>
      <p>This folder is empty.</p>
    <% } %>

    <% entries.forEach(entry => { %>
      <div class="item">
        <div class="name">
          <a href="<%= entry.isDir ? '/files?path=' + entry.encodedPath : '/file?path=' + entry.encodedPath %>">
            <%= entry.name %>
          </a>
          <span class="type">[<%= entry.isDir ? 'folder' : 'file' %>]</span>
        </div>
        <% if (!entry.isDir) { %>
          <a href="/download?path=<%= entry.encodedPath %>">⬇️ Download</a>
        <% } %>
      </div>
    <% }); %>
  </body>
</html>
